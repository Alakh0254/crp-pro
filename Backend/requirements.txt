annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.13.0
click==8.4.1
colorama==0.4.6
fastapi==0.136.3
greenlet==3.5.1
h11==0.16.0
# Pinned <0.28: starlette 0.27's TestClient uses httpx's app= shortcut, which
# httpx 0.28 removed. Bump this only together with starlette/fastapi.
httpx==0.27.2
idna==3.17
# Password hashing (Phase 3 auth). We use passlib's pure-Python pbkdf2_sha256
# scheme, so no native bcrypt build is required. JWTs are signed with the Python
# standard library (hmac/hashlib) in auth.py, so python-jose is intentionally not
# a dependency here — swap to bcrypt/jose later if desired.
passlib==1.7.4
# Lets FastAPI read form fields (username/password) for the OAuth2 login route.
python-multipart==0.0.29
pydantic==2.13.4
pydantic_core==2.46.4
SQLAlchemy==2.0.50
starlette==1.2.0
typing-inspection==0.4.2
typing_extensions==4.15.0
uvicorn==0.48.0
